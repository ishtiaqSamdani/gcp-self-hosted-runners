name: Current VM Information 1

on:
  push:
    # workflow_dispatch:

jobs:
  vm_info:
    runs-on: flash-2cpu-4GB
    steps:
    - name: Display current VM information
      run: |
        sleep 60
        hostname
